# Atlas Configuration
# Change these values for your specific environment

# Panel Port (Internal to the server)
ATLAS_PANEL_PORT=4000

# Core API Port (Internal to the server)
ATLAS_CORE_PORT=8080

# SFTP Port (Publicly exposed for clients)
ATLAS_SFTP_PORT=2022

# Node Port (Communication between Core and Daemon)
ATLAS_NODE_PORT=8081

# Database Credentials
DB_USER=atlas_admin
DB_PASS=change_me_immediately
DB_NAME=atlas

# Security & Authentication
# Note: The Node Token is generated by the Panel. 
# 1. Start the Core/Panel first.
# 2. Go to Admin > Nodes and create a new node.
# 3. Copy the token generated and paste it here.
NODE_TOKEN=paste_your_node_token_here
